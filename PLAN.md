# AtherNull — Implementation Plan

Source spec: `Proto_col_Architecture_Research_Edition-1.pdf` (Research Edition, DRAFT v1.0, 2026-09-18) —
"Proto-col" was only the codename used in that research document; the project itself is AtherNull.
This file tracks what's been validated against that spec, what's still open, and the next concrete
steps. Update it as decisions get made — don't let it drift into a second copy of the spec.

## Status legend
`[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked / needs a decision

---

## 0. Validated against research (2026-09-19)

These confirm or adjust the spec's §11 "proposed defaults":

| Decision | Spec default | Research finding | Action |
|---|---|---|---|
| SDK integration | Pinned Python dependency | Confirmed. `pip install -U openhands-sdk openhands-tools` (matched pair, Python ≥3.12), MIT licensed. `openhands.workspace` already ships Local/Docker/Remote sandbox abstractions matching §6. Verified live: real install of pinned 1.42.1 + `from openhands.sdk import LLM, Agent, Conversation, Tool` imports clean. | Proceed as planned. |
| Sandbox isolation | "Dedicated host or stronger VM/microVM isolation" (undecided) | Plain `DockerWorkspace` isolation is undocumented/unhardened — treat as container-level only, not a security boundary. Firecracker/Kata need `/dev/kvm` + nested virtualization, which most non-bare-metal VPS don't expose (AWS needs `.metal`; DO/Azure/GCP often support it — **must confirm with Coolify VPS provider**). gVisor (`runsc`) runs as a Docker runtime swap, no bare metal needed, is the standard recommendation for shared multi-tenant untrusted-code hosts. | **[!] Decide**: gVisor-hardened Docker for MVP; Firecracker only if KVM/nested-virt confirmed available later. Daytona's "Runtime for OpenHands" is a hosted fallback to unblock the demo without building sandbox infra at all. |
| Model selection | Single `model` per agent profile | User requirement (2026-09-19): not locked to one provider, and must route to cheaper/pricier models by task complexity automatically, not just let clients hand-pick a model. | **Decided**: ADR-0003 (`docs/adr/0003-cost-complexity-model-routing.md`). `agent_profiles.model` → `model_tiers`; routing computed by the orchestrator/API at dispatch time via `packages/model-router` (v1 rule-based heuristic, unit tested), never inside the worker. |
| Auth / identity | Not specified in spec §11 | User requirement (2026-09-19): needed a real org/session/membership layer before Phase 1's tables (which all assume `organization_id`) mean anything. | **Decided**: ADR-0004 (`docs/adr/0004-better-auth-for-identity.md`). Better Auth owns identity via its `organization` plugin on the same Postgres instance (Kysely+`pg.Pool`, no new ORM); identity schema is generated by its CLI, never hand-written. Verified live end-to-end, see Phase 0.5 below. |

Open from spec §11 still needing validation: backend runtime choice (TypeScript API — team preference, not yet decided), database isolation approach beyond RLS testing, verification authority/dispute process owner, Solana payments (devnet contract not yet written/audited), repository access consent flow, retention TTLs.

---

## 1. Non-negotiables (carry into every phase, don't relitigate)

- Agent never gets host SSH, Docker socket, prod DB creds, or escrow signing keys.
- PostgreSQL is the money/state source of truth; Redis is queue/cache only.
- A worker reports results; it cannot self-approve or release funds.
- Tenant isolation enforced server-side on every read/write/download — never trust a client-supplied tenant ID.
- No deployment or payment counts as successful until the authoritative system confirms it.

---

## 2. Build phases (from spec §10, adjusted)

- [~] **Phase 0 — Repo and decisions**
  - [x] Scaffold repo layout (see §3 below) — `pnpm install`, `pnpm -r typecheck`, `pnpm -r build` all verified green (2026-09-19)
  - [x] ADR: OpenHands SDK as pinned dependency, not fork — pin exact version (`docs/adr/0001-openhands-sdk-as-dependency.md`, pinned 1.42.1)
  - [~] ADR: sandbox isolation choice (gVisor vs Docker-only vs Daytona-hosted for MVP) — written (`docs/adr/0002-sandbox-isolation-strategy.md`) but Status: Proposed, blocked on confirming Coolify VPS KVM/nested-virt support
  - [~] Threat model doc in `docs/` — skeleton in place (`docs/threat-model.md`), fill in per-phase
  - [x] Baseline CI (`.github/workflows/ci.yml` — node typecheck/build matrix + python install/ruff matrix)
- [x] **Phase 0.5 — Identity (Better Auth)** — not in the original spec's phase list; inserted because Phase 1's tables all assume a real `organization_id`/user concept that didn't exist yet. See `docs/adr/0004-better-auth-for-identity.md`.
  - [x] Better Auth wired into `apps/api` (`src/auth.ts`, mounted at `/api/auth/*` in `src/index.ts`) — Postgres via its built-in Kysely+`pg.Pool` adapter, `generateId: "uuid"`, `organization` plugin enabled.
  - [x] `packages/database/migrations/0001_better_auth_schema.sql` — generated for real via `pnpm dlx auth@1.7.5 generate` against a live DB, not hand-written. Defines `user`/`session`/`account`/`verification`/`organization`/`member`/`invitation`.
  - [x] `packages/database/migrations/0001_init.sql` renamed → `0002_platform_tables.sql`; hand-rolled `organizations`/`memberships` tables deleted; every `organization_id` FK repointed at `"organization"`; `projects.owner_member_id` → `owner_user_id` repointed at `"user"`.
  - [x] Verified live, not just compiled: both migrations applied in order against a real local Postgres with zero FK errors; `apps/api` booted for real and `/health` (200), `/api/auth/get-session` (200, null), `/api/auth/organization/list` (401 anonymous) all responded correctly through the Fastify mount.
  - [ ] No sign-in method enabled yet (email/password, OAuth, etc.) — open decision, not blocking Phase 1.
  - [ ] `apps/web` client-side session/org wiring — deliberately deferred to Phase 6.
- [~] **Phase 1 — Core job engine**
  - [x] PostgreSQL migrations for `organizations`, `tasks`, `executions`, `usage_events` (schema in spec §5) — now split across `0001_better_auth_schema.sql` (identity) + `0002_platform_tables.sql` (platform), see Phase 0.5. Includes `agent_profiles.model_tiers` and `executions.routing_*` columns (ADR-0003).
  - [ ] Task FSM: `CREATED → AWAITING_FUNDING → FUNDED → QUEUED → RUNNING → VERIFYING → AWAITING_ACCEPTANCE → ACCEPTED → SETTLING → SETTLED` (+ `FAILED`/`CANCELLED`/`REJECTED`/`DISPUTED`/`REFUNDING→REFUNDED`)
  - [ ] API + queue wiring — `apps/api` has `/health` + Better Auth's `/api/auth/*`, but no job endpoints yet
  - [ ] Wire `packages/model-router`'s `chooseTier()` into task dispatch so `InternalWorkDispatch.resolvedModel`/`routingTier`/`routingScore`/`routingReason` are actually populated, not just schema fields
  - [ ] Exit test: job survives API/worker restart without duplicate execution
- [~] **Phase 2 — OpenHands adapter**
  - [x] Minimal spike: SDK agent edits a disposable test repo — ran live 2026-09-19 against `nvidia_nim/nvidia/nemotron-3-super-120b-a12b` (NVIDIA Build). Agent added `add(a, b)` to `src/math_utils.py`, wrote `test_math_utils.py` itself, ran it via the terminal tool, confirmed exit 0 — independently re-verified outside the agent's own claim. First model tried (`qwen/qwen3-coder-480b-a35b-instruct`) had hit NVIDIA's EOL; swapped for a currently-live catalog entry.
  - [ ] Wrap SDK calls behind an internal adapter (spec §7 internal work dispatch contract) so upgrades don't touch the public API
  - [ ] Event capture → `usage_events`, `CHECKPOINT_SAVED`
  - [x] Exit test: produces a patch on an approved test repository — met by the spike above
- [ ] **Phase 3 — Isolated execution**
  - [ ] Stand up chosen sandbox (gVisor-hardened `DockerWorkspace` per Phase 0 ADR)
  - [ ] Quotas: CPU, memory, wall time, spend cap enforced outside the agent
  - [ ] Default-deny outbound network; allowlist package registries
  - [ ] Exit test: no host or sibling-tenant access; cleanup verified
- [ ] **Phase 4 — Independent verification**
  - [ ] Verifier service: fresh checkout, deterministic tests, evidence report — never trusts agent's own success claim
  - [ ] Exit test: a failing test blocks acceptance and payout
- [ ] **Phase 5 — Payment integration**
  - [ ] Solana devnet escrow: fund / release / refund, idempotent settlement intents
  - [ ] Exit test: real testnet transactions; retries don't double-pay
- [ ] **Phase 6 — Customer experience**
  - [ ] Task form, live status, artifact/result review, wallet state in Next.js UI
  - [ ] Exit test: user completes full workflow without an operator console
- [ ] **Phase 7 — Reliability and demo**
  - [ ] Backup/restore drills, crash tests, spend-cap tests, demo recordings
  - [ ] Exit test: demo works repeatedly, including the failure/refund path

**Go/no-go gate** (spec §10): don't add marketplace bidding, multi-chain, autonomous prod deploy, or more agent types until one coding job reliably produces a verified patch within budget and survives retries.

---

## 3. Repository layout (spec §3)

```
apps/web/               # Next.js
apps/api/                # Backend
workers/coding-agent/    # Python + OpenHands SDK
services/verifier/       # Independent checks
packages/database/       # Schema and migrations
packages/contracts/      # API/event schemas
packages/model-router/   # Cost/complexity-based model routing (ADR-0003, not in original spec layout)
contracts/solana/        # Optional escrow program
infrastructure/          # Compose, Coolify, monitoring
docs/                     # ADRs, runbooks, threat model
```

---

## 4. Immediate next tickets (this week)

1. [x] Create this repo layout + baseline CI — done 2026-09-19
2. [x] ADR-001: SDK as dependency, not fork — pin tested `openhands-sdk`/`openhands-tools` release (1.42.1)
3. [~] ADR-002: sandbox isolation choice for MVP (gVisor vs Daytona-hosted) — drafted, **needs Coolify VPS provider's KVM/nested-virt support confirmed before Firecracker can even be considered**
4. [ ] OpenHands spike: agent edits a disposable example repo end-to-end — next up
5. [x] Define `Job`(tasks), `Execution`, `AgentProfile`, `Artifact` schemas with tenant IDs (spec §5) — `packages/database/migrations/0001_init.sql`
6. [~] Define the internal execution request/result contract (spec §7) — request shape done (`packages/contracts/src/jobs.ts`), result/event payload shapes still open
7. [x] Clean verifier stub: runs tests on a fresh checkout, no shared state with the agent run — `services/verifier` scaffolded, no dependency on the worker package

---

## 5. Decisions still needed from you

- [ ] Confirm Coolify VPS provider/instance type → determines whether Firecracker is viable or gVisor/Daytona is the ceiling for now
- [ ] Backend runtime: confirm TypeScript for Core API (spec's default) or something else
- [ ] Who owns verification/dispute acceptance decisions in the MVP (a human reviewer, or fully automated on test pass)?
- [ ] Confirm Solana devnet as the only settlement target for the first demo (per spec §8 scope decision — no Meteora/token launch/Zcash)
- [ ] Which sign-in method(s) should Better Auth actually offer (email/password, OAuth providers, magic link)? Currently none are enabled — `apps/api/src/auth.ts` only configures the database and the organization plugin.
