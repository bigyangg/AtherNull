# Runbooks

Empty until there's something real to operate. Spec §9 lists the required runbook essentials
(pause new jobs, worker crash recovery, payment uncertainty, suspected cross-tenant access, backup
tests) — write these as each phase makes them concrete, not speculatively.
